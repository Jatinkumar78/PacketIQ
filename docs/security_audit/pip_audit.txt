pip-audit — PacketIQ reference venv (Python 3.12.13) — 2026-07-15

The dev/reference environment was migrated from Python 3.9.6 to a standalone
uv-managed CPython 3.12.13 (no system change, no code change; requires-python
stays ">=3.9"). On 3.12 the pinned security floors resolve to fully-patched
releases, clearing the runtime-dependency advisories that remained on the 3.9
ceiling (python-multipart, starlette, requests, urllib3, click, python-dotenv).

Resolved runtime versions: python-multipart 0.0.32, requests 2.34.2,
urllib3 2.7.0, starlette 1.3.1, click 8.4.2, python-dotenv 1.2.2.

Residual: a single dev-only transitive advisory in diskcache (pulled by the
dev-extra pySigma, used for SIGMA-rule tests — NOT a runtime dependency, so it
is never installed by "pip install packetiq"). PYSEC-2026-2447 has no upstream
fix released yet.

---------------------------------------------------------------------
Found 1 known vulnerability in 1 package
Name      Version ID              Fix Versions
--------- ------- --------------- ------------
diskcache 5.6.3   PYSEC-2026-2447

Name     Skip Reason
-------- -----------------------------------------------------------------------
packetiq Dependency not found on PyPI and could not be audited: packetiq (1.0.0)
